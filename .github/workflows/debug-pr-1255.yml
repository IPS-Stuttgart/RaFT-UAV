---
name: Debug PR 1255 candidate reservoir

"on":
  pull_request:
  workflow_dispatch:

jobs:
  focused-test:
    runs-on: ubuntu-latest
    steps:
      - name: Clone branch and run focused regression
        run: |
          git clone --quiet --depth 1 --branch agent/scope-candidate-reservoir-csv-reader \
            https://github.com/IPS-Stuttgart/RaFT-UAV.git repo
          cd repo
          python -m pip install --quiet -e ".[dev]"
          pytest -vv tests/test_mmuad_candidate_reservoir_sequence_text.py --maxfail=1
