line-length = 100

[lint]
select = ["E4", "E7", "E9", "F"]

[lint.per-file-ignores]
# The cross-fitted evidence runner performs a deliberate fail-fast dataset-root
# preflight before candidate callbacks receive the same validated path.
"scripts/run_multi_uav_lts_cross_fitted_edge_evidence.py" = ["F841"]
